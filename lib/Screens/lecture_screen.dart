import 'package:flutter/material.dart';

class LecturePage extends StatelessWidget {
  const LecturePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lecture Page Content', style: TextStyle(fontSize: 24)),
    );
  }
}